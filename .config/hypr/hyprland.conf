
# =============================================================================
#  STARTUP APPS
# =============================================================================
exec-once = dbus-update-activation-environment --systemd --all
exec-once = systemctl --user start hyprland-session.target
exec-once = ~/.local/bin/dms-sync &
exec-once = sleep 1 && kitty --class kitty-dropdown --title kitty-dropdown
exec-once = /usr/bin/kdeconnectd &

# =============================================================================
#  INPUT CONFIG
# =============================================================================
input {
    kb_layout = us
    numlock_by_default = true
}

# =============================================================================
#  GENERAL & LAYOUT (Overrides)
# =============================================================================
general {
    gaps_in = 15
    gaps_out = 25
    border_size = 0
}

dwindle {
    preserve_split = true
    force_split = 2
}

# =============================================================================
#  DECORATION
# =============================================================================
decoration {
    rounding = 15
	
    active_opacity = 0.80
    inactive_opacity = 0.80
    
	
	 blur {
        enabled = true
        size = 3
        passes = 1
        new_optimizations = true
        ignore_opacity = true
        noise = 0
        brightness = 0.60
        ignore_opacity = true # Enables blur behind semi-transparent windows [1]
        xray = false       # Set to true to see wallpaper through windows [1]
    }
    shadow {
        enabled = true
        range = 30
        render_power = 5
        offset = 0 0
        color = rgba(00000050)
    }
}

# =============================================================================
#  FLOW ANIMATIONS (The 2026 Meta)
# =============================================================================
animations {
    enabled = true

    # Custom Bezier for the Flow effect
    bezier = fluent_decel, 0, 0.2, 0.4, 1

    # Window Animations
    animation = windowsIn, 1, 3, default, popin 80%
    animation = windowsOut, 1, 3, default, popin 80%
    animation = windowsMove, 1, 4, default

    # The Flow Meta: Workspaces move and fade simultaneously
    animation = workspaces, 1, 4, fluent_decel, slidefade 20%

    # Special Workspace (kitty-dropdown)
    animation = specialWorkspace, 1, 3, default, slidevert

    # UI Elements
    animation = fade, 1, 3, default
    animation = border, 1, 3, default
}

# =============================================================================
#  MISC
# =============================================================================
misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    vrr = 1
    vfr = true
}

layerrule = no_anim on, match:namespace ^(quickshell)$

# =============================================================================
#  SOURCE FILES (Loading DMS Defaults)
# =============================================================================
source = ./dms/colors.conf
source = ./dms/outputs.conf
# source = ./dms/layout.conf
source = ./dms/cursor.conf
source = ./dms/binds.conf
source = ./dms/env.conf
source = ./dms/rules.conf
